/* Commands to set up the necessary database and tables */

CREATE DATABASE sporks50_char;
USE sporks50_char;
CREATE TABLE IF NOT EXISTS cp_names ( ID MEDIUMINT PRIMARY KEY NOT NULL AUTO_INCREMENT, URI TEXT, Page FLOAT(8.2) );
CREATE TABLE IF NOT EXISTS cp_contents ( ID MEDIUMINT PRIMARY KEY NOT NULL AUTO_INCREMENT, Page FLOAT(8.2), Content MEDIUMTEXT );
CREATE TABLE IF NOT EXISTS cp_items ( ID MEDIUMINT PRIMARY KEY NOT NULL AUTO_INCREMENT, Name TEXT, Page FLOAT(8.2), Description LONGTEXT );
CREATE TABLE IF NOT EXISTS cp_styles ( ID MEDIUMINT PRIMARY KEY NOT NULL AUTO_INCREMENT, Type TINYTEXT, Name TINYTEXT, Page FLOAT(8.2), Description LONGTEXT );
CREATE TABLE IF NOT EXISTS cp_types ( ID MEDIUMINT PRIMARY KEY NOT NULL AUTO_INCREMENT, Name TINYTEXT, Type TINYTEXT );
CREATE USER 'sporks50_char'@'localhost' IDENTIFIED BY 'bBpA?7h%s#w~XC';
GRANT ALL PRIVILEGES ON sporks50_char.* TO 'sporks50_char'@'%';
FLUSH PRIVILEGES;










CREATE TABLE IF NOT EXISTS cp_name ( ID MEDIUMINT PRIMARY KEY NOT NULL AUTO_INCREMENT, URI TEXT, Page MEDIUMINT );
CREATE TABLE IF NOT EXISTS cp_content ( Page MEDIUMINT PRIMARY KEY, Content MEDIUMTEXT );
CREATE TABLE IF NOT EXISTS cp_items ( ID MEDIUMINT PRIMARY KEY NOT NULL AUTO_INCREMENT, Name TEXT, Page MEDIUMINT, DispName TEXT, Image TEXT, Description LONGTEXT );
CREATE TABLE IF NOT EXISTS cp_styles ( ID MEDIUMINT PRIMARY KEY NOT NULL AUTO_INCREMENT, Type TINYTEXT, Name TINYTEXT, Page MEDIUMINT, SingleStyle TINYTEXT, Start LONGTEXT, End LONGTEXT, Middle LONGTEXT, LastLink LONGTEXT, NextLink LONGTEXT );
CREATE USER 'sporks50_char'@'localhost' IDENTIFIED BY 'bBpA?7h%s#w~XC';
GRANT ALL PRIVILEGES ON sporks50_char.* TO 'sporks50_char'@'%';
FLUSH PRIVILEGES;

/* all single quotes and backslashes need to be escaped */
/* Within JSON all double quotes need to be double escaped */
/* Commands to insert into the cp_name table */

INSERT INTO sporks50_char.cp_name ( URI, Page ) VALUES (
	'',
	''
);


/* Commands to insert into the cp_content table */

INSERT INTO sporks50_char.cp_content ( Page, Content ) VALUES (
	'',
	''
);


/* Commands to insert into the cp_items table */

INSERT INTO sporks50_char.cp_items ( Name, Page, DispName, Image, Description ) VALUES (
	'',
	'',
	'',
	'',
	''
);


/* Commands to insert into the cp_content table */

INSERT INTO sporks50_char.cp_styles ( Type, Name, Page, SingleStyle, Start, End, Middle, LastLink, NextLink ) VALUES (
	'',
	'',
	'',
	'',
	'',
	'',
	'',
	'',
	''
);
